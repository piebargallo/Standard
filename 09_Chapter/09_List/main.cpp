#include <complex>
#include <vector>
#include <string>
#include <map>

int a = { 1 };
std::complex<double> z{ 1,2 };
std::vector<std::string> b{ "once", "upon", "a", "time" }; // 4 strings elements
std::map<std::string, int> c{ {"bear", 4} };